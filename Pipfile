[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[requires]
python_version = "3.12"

[packages]
# Pin to specific version for reproducibility
# Lambda runtime provides boto3, but we pin for local dev consistency
boto3 = "==1.40.55"

[dev-packages]
pytest = "*"
pytest-playwright = "*"
black = "*"
ruff = "*"
mypy = "*"
requests = "*"  # For populate-authors.py script
